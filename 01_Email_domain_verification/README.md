# MX Checker - Проверка MX-записей доменов

Простой скрипт на Python для проверки наличия MX-записей у доменов email-адресов.
Скрипт создан для ознакомления с базовыми возможностями Python.

## Возможности

- 📧 Чтение email-адресов из файла
- 🔍 Проверка существования домена
- 📝 Проверка наличия MX-записей
- 💾 Сохранение результатов в файл
- 📊 Вывод результатов в консоль

## Установка

### 1. Клонирование/скачивание
Скопируйте файл `email_checker.py` в нужную директорию.

### 2. Установка зависимостей
Установите необходимую библиотеку:

```bash
pip install dnspython
```

Или используйте файл requirements.txt (создайте его в той же директории):
```
dnspython==2.4.2
```

И выполните:
```bash
pip install -r requirements.txt
```

## Использование

### 1. Подготовка файла с email-адресами

Создайте файл `in_email.txt` в той же директории, где находится скрипт.
Добавьте email-адреса по одному на строку:
Пример:

```
user1@gmail.com
user2@yandex.ru
test@example.com
admin@company.org
invalid@nonexistent-domain-12345.com
bad-email-format
```

### 2. Запуск скрипта

```bash
python email_checker.py
```

### 3. Просмотр результатов

Результаты будут выведены в консоль и сохранены в файл `out_email.txt`.

## Пример работы

### Консольный вывод:
```
Найдено 6 email-адресов для проверки
--------------------------------------------------
1. user1@gmail.com: домен валиден
2. user2@yandex.ru: домен валиден
3. test@example.com: домен валиден
4. admin@company.org: домен валиден
5. invalid@nonexistent-domain-12345.com: домен отсутствует
6. bad-email-format: некорректный email-адрес
--------------------------------------------------
Результаты сохранены в файл: /path/to/directory/out_email.txt
```

### Содержимое файла out_email.txt:
```
Результаты проверки MX-записей:
==================================================
user1@gmail.com: домен валиден
user2@yandex.ru: домен валиден
test@example.com: домен валиден
admin@company.org: домен валиден
invalid@nonexistent-domain-12345.com: домен отсутствует
bad-email-format: некорректный email-адрес
```

## Возможные статусы проверки

| Статус | Описание |
|--------|----------|
| `домен валиден` | Домен существует и имеет корректные MX-записи |
| `домен отсутствует` | Домен не зарегистрирован или не существует |
| `MX-записи отсутствуют или некорректны` | Домен существует, но не настроен для приема почты |
| `некорректный email-адрес` | Некорректный формат email-адреса |

## Структура проекта

```
01_Email_domain_verification/
├── email_checker.py     # Основной скрипт
├── in_email.txt         # Входной файл с email-адресами
├── out_email.txt        # Выходной файл с результатами (создается автоматически)
├── requirements.txt     # Файл зависимостей (опционально)
├── LICENSE.md          # Лицензия на использование скрипта
└── README.md            # Эта инструкция

```

## Требования

- Python 3.6 или выше
- Библиотека `dnspython`

## Ограничения

1. Скрипт не проверяет валидность формата email-адреса (только наличие символа @)
2. Проверка MX-записей зависит от доступности DNS-серверов
3. Некоторые домены могут блокировать DNS-запросы
4. Проверка может занимать время при большом количестве адресов


## Поддержка

При возникновении проблем:
1. Убедитесь, что установлена библиотека `dnspython`
2. Проверьте, что файл `in_email.txt` существует в той же директории
3. Убедитесь, что есть доступ к интернету для DNS-запросов
4. Проверьте права на запись в директории

## Автор

Скрипт создан для автоматизации проверки MX-записей доменов.


## 📄 Лицензия

Этот проект распространяется под [Пользовательской лицензией](LICENSE.md).
Полный текст лицензии доступен в файле `LICENSE.md`.
